#try #3 on nov 5th

#install and make sure versions are completely correct
#need to do within environment

#create and activate environment
conda create symcount

conda activate symcount
#navigate to the folder where you are doing work

#run roi_prepare_from_xml_v2.py script
#with new training set
#this prompts the top left and bottom right square from ROI

python roi_prepare_from_xml_v2.py \
  --in_dir  ~/Documents/sym_counting_oki/training_dataset \
  --out_dir ~/Documents/sym_counting_oki/yolo_data_b16 \
  --roi_json ~/Documents/sym_counting_oki/roi_cache.json \
  --box_um 10 \
  --val_frac 0.15 \
  --filter_to_roi true
  
#and then outputs the overlays with truth

yolo_data_b16/
  images/{train,val}/
  labels/{train,val}/
  overlays/<image>_truth.png   # quick visual QC
  sym_dataset.yaml

#If boxes look a bit small/large, re-run with --box_um 10 or --box_um 14
#ended up using box_um = 10 which looked best based on images

#train a small detector (ultralytics)

export PYTHONNOUSERSITE=1   # avoid user-site packages leaking in

#install pandas
pip install pandas

#for the rest of the installation
# 0) sanity: confirm we’re in the right interpreter
python -c "import sys; print(sys.executable)"
#this should show you the correct filepath

# 1) nuke the troublemakers (order matters) (in theory you shouldnʻt have anything installed but just in case)
python -m pip uninstall -y ultralytics torch torchvision opencv-python opencv-python-headless numpy

# 2) pin NumPy to 1.x FIRST (prevents later upgrades)
python -m pip install "numpy==1.26.4"

# 3) install a CPU torch build that works with NumPy 1.x
python -m pip install --no-cache-dir \
  "torch==2.2.2" "torchvision==0.17.2" \
  --index-url https://download.pytorch.org/whl/cpu

# 4) install Ultralytics WITHOUT pulling deps (so it can’t bump numpy)
python -m pip install --no-deps "ultralytics==8.3.225"

# 5) install exactly ONE OpenCV (headless is safest for servers/conda)
python -m pip install "opencv-python-headless==4.10.0.84" matplotlib PyYAML tqdm psutil

# 6) verify versions (these must print successfully)
python - <<'PY'
import numpy, torch, ultralytics, cv2
print("numpy", numpy.__version__)
print("torch", torch.__version__)
print("ultralytics", ultralytics.__version__)
print("opencv", cv2.__version__)
PY

#these are the versions you should have:
#numpy 1.26.4
#torch 2.2.2
#ultralytics 8.3.225
#opencv 4.10.0

#train the yolo model!
#this will take a long time - ~5 hours on my computer
#you can also probably do this in a cluster and it would be better/more efficient
#epochs are the # of iterations it will do

export OMP_NUM_THREADS=1
RUNNAME=train_y8s_e100_b16_1280_2Sept2026

yolo detect train \
  model=yolov8s.pt \
  data=~/Documents/sym_counting_oki/yolo_data_b16/sym_dataset.yaml \
  imgsz=1280 \
  epochs=100 \
  batch=4 \
  mosaic=0 degrees=5 translate=0.03 scale=0.12 shear=0.0 \
  hsv_h=0.0 hsv_s=0.12 hsv_v=0.12 \
  fliplr=0.0 \
  workers=0 \
  patience=50 \
  name=$RUNNAME
  
#If recall is low, increase epochs to 120 and/or imgsz to 1280
#this stopped getting better after 85 iterations so just do 100 epochs
#12/3 Ran for 6.772 hours with larger training dataset (30 images with counts, at least 4 images per species)
#12/8 120 epochs completed in 8.739 hours with larger training dataset, 3 image pairs (6 images per species) = 42 images total
#2 July 2026 - running train_y8s_e100_b16_1280_2July2026, 60 images total, 100 epochs, keeping imgsz 1280, started at 9:20am July 2nd, 100 epochs completed in 4.762 hours!!! oooh
#2 September 2026, 100 epochs completed in 10.947 hours.


#now actually run the detector to see how it works!
#from before: RUNNAME=train_y8s_e100_b16_1280_2July2026

python infer_and_count.py \
  --in_dir  ~/Documents/sym_counting_oki/training_dataset \
  --out_dir ~/Documents/sym_counting_oki/detections_y8s_e100_b16_1280_2Sept2026 \
  --roi_json ~/Documents/sym_counting_oki/roi_cache.json \
  --weights  ~/Documents/sym_counting_oki/runs/detect/$RUNNAME/weights/best.pt \
  --imgsz 1280 --conf 0.35 --iou 0.50
  
#ran this and it underestimated - lowering conf to 0.35, now 0.3 from 0.4 and trying again
#ran this detector to see which conf was best - keep at 0.35

#now use this to count new photos
python infer_and_count.py \
  --in_dir  ~/Documents/sym_counting_oki/new_images_to_count \
  --out_dir ~/Downloads/sym_counting_oki/detections_final \
  --roi_json ~/Downloads/sym_counting_oki/roi_cache.json \
  --weights  ~/Downloads/sym_counting_oki/runs/detect/train_y8s_e100_b16_1280_2July2026/weights/best.pt \
  --imgsz 1280 \
  --conf 0.35 \
  --iou 0.5 \
  --tta
  
#tta is an additional boolean flag that enables time-test augmentation
#still getting 96% correct overall

#now run for real
#run in batches of ~200 samples and clear persistent_cache between each one
python infer_and_count.py \
  --in_dir  ~/Documents/sym_counting_oki/all_images_batch6 \
  --out_dir ~/Documents/sym_counting_oki/all_image_detections_batch6 \
  --roi_json ~/Documents/sym_counting_oki/roi_cache.json \
  --weights  ~/Documents/sym_counting_oki/runs/detect/train_y8s_e100_b16_1280_2Sept2026/weights/best.pt \
  --imgsz 1280 \
  --conf 0.35 \
  --iou 0.5 \
  --tta

#while I was running this it stopped mid-run because I ran out of space
#i had an issue with the cache so had to remove it here:

rm -f "$HOME/Library/Application Support/Ultralytics/persistent_cache.json"

#then checked packages within symcounts conda environment to make sure they were still good
python - <<'PY'
import numpy, torch, ultralytics, cv2
print("numpy", numpy.__version__)
print("torch", torch.__version__)
print("ultralytics", ultralytics.__version__)
print("opencv", cv2.__version__)
PY

##use this script to evaluate the training using the xml files from cell counter
#12/3 changed to use e100 instead of e180 for this one for new evaluation
#12/8 trying with e180 instead

python evaluate_spatial.py \
  --in_dir  ~/Documents/sym_counting_oki/training_dataset \
  --roi_json ~/Documents/sym_counting_oki/roi_cache.json \
  --weights  ~/Documents/sym_counting_oki/runs/detect/train_y8s_e100_b16_1280_2Sept2026/weights/best.pt \
  --imgsz 1280 --conf 0.35 --iou 0.5 --tta \
  --match_um 6 \
  --out_csv ~/Documents/sym_counting_oki/eval_spatial_training_2sept2026.csv \
  --out_dir ~/Documents/sym_counting_oki/eval_spatial_debug_2sept2026 \
  --save_overlays
  
#and in case you have to reclick the ROI for any individual images, add --reclick argument

python evaluate_spatial.py \
  --in_dir  ~/Downloads/sym_counting_oki/training_dataset \
  --roi_json ~/Downloads/sym_counting_oki/roi_cache.json \
  --weights  ~/Downloads/sym_counting_oki/runs/detect/train_y8s_e180_b16_1280/weights/best.pt \
  --imgsz 1280 --conf 0.35 --iou 0.5 --tta \
  --match_um 6 \
  --reclick P8174886 \
  --out_csv ~/Downloads/sym_counting_oki/eval_spatial_training14.csv \
  --out_dir ~/Downloads/sym_counting_oki/eval_spatial_debug \
  --save_overlays
  
#trying to run for just one sample

python infer_and_count.py \
  --in_dir  ~/Downloads/sym_counting_oki/image_test \
  --out_dir ~/Downloads/sym_counting_oki/all_image_detections_batch1/test \
  --roi_json ~/Downloads/sym_counting_oki/roi_cache.json \
  --weights  ~/Downloads/sym_counting_oki/runs/detect/train_y8s_e180_b16_1280/weights/best.pt \
  --imgsz 1280 \
  --conf 0.35 \
  --iou 0.5 \
  --tta
  
  
##Here's how it works:

roi_prepare_from_xml_v2.py (dataset & ROI prep)

Purpose: turn your manually annotated images (Fiji CellCounter XML + raw JPG) into a clean YOLO dataset and an ROI cache.

What it does

Reads CellCounter XML (CellCounter_<stem>.xml), pulls Type-1 markers (your blue-dot truths), and pairs them to the matching raw image.

Builds/updates an ROI cache (roi_cache.json).

If an ROI is missing or invalid, it can prompt you to click top-left and bottom-right of the 5×5 grid (1 mm wide → 25 squares).

Transforms ground-truth points to YOLO labels inside the ROI:

Converts dot annotations to small boxes centered on each dot (YOLO needs boxes, not points).

Writes one labels/<stem>.txt per image (class, x_center, y_center, width, height in normalized ROI coordinates).

Splits images and labels into yolo_data/train/ and yolo_data/val/ (stratified or simple split).

(Optional) QA overlays so you can visually verify ROI and label placement.

Key inputs: raw JPGs, CellCounter_*.xml.
Outputs: yolo_data/{images,labels}/{train,val}, roi_cache.json, optional overlays.

Knobs you can tweak

Train/val split fraction.

The box size used to convert dots → YOLO boxes (slightly larger boxes can help the detector learn tiny objects).

Whether to re-click ROIs or reuse cached ones.

yolo detect train (model training)

Purpose: train a detector to find symbiont cells inside the ROI crops.

What it does

Loads a YOLOv8 model (e.g., yolov8s.pt or yolov8n.pt) and your sym_dataset.yaml pointing to yolo_data/.

Trains for N epochs with your chosen image size and augmentations.

Typical command

yolo detect train \
  model=yolov8s.pt \
  data=.../yolo_data/sym_dataset.yaml \
  imgsz=1280 \
  epochs=180 \
  batch=16 \
  mosaic=0 degrees=5 translate=0.05 scale=0.10 hsv_h=0.0 hsv_s=0.15 hsv_v=0.15 fliplr=0.0 \
  workers=0


Why these settings

imgsz=1280: more pixels helps with small, round cells.

mosaic=0 and gentle geometric/color augs: preserve the fine texture and roundness; avoid heavy mosaics that distort scale.

Small model (y8n/y8s): good balance for CPU/mac training.

Outputs

runs/detect/train/* with best.pt, plots (results.png, labels.jpg), tensorboard logs, and validation metrics.

What to monitor

Val plots and labels.jpg (sanity check).

If the model undercounts, consider:

Lower conf or increase box size during label generation,

Train longer or move from y8n → y8s → y8m if you have compute,

Add more diverse training images.

infer_and_count.py (production inference & counting)

Purpose: run the trained model on new images, restricted to the 5×5 ROI, and save both overlays and a counts.csv.

What it does

Loads ROI cache (roi_cache.json). If an image is missing or has a bad ROI, it prompts you to click TL/BR of the 5×5 grid (with R to reuse last box).

Crops the image to the ROI, runs YOLO, and collects detections.

Writes an overlay (raw image + red ROI box + green boxes) and appends a row to counts.csv with image_id and count.

Supports --tta (test-time augmentation) to boost recall for faint/partial cells.

Typical command

python infer_and_count.py \
  --in_dir  ~/.../new_images \
  --out_dir ~/.../detections_final \
  --roi_json ~/.../roi_cache.json \
  --weights  ~/.../runs/detect/train_y8s_e180_b16_1280/weights/best.pt \
  --imgsz 1280 \
  --conf 0.35 \
  --iou 0.5 \
  --tta


Key parameters

--conf: confidence threshold; your runs suggested 0.35–0.40 is the sweet spot (lowest MAE).

--tta: on for higher recall (slower).

ROI cache is written atomically to avoid corruption; you can re-pick any image’s ROI as needed.

Outputs

Overlays: <stem>_det.png (raw image with ROI and detections).

counts.csv: tidy table for downstream merging.

How the pieces fit together

roi_prepare_from_xml_v2.py
builds clean training data + ROI cache from your Fiji annotations.

yolo detect train
learns to detect symbionts within ROI crops using your curated labels.

infer_and_count.py
applies the trained detector to new ROIs, saves overlays, and counts.

Optional evals you have:

evaluate_training_set.py: quick count-level accuracy against XML.

evaluate_spatial.py: spatial TP/FP/FN by matching detections to ground-truth points (with ROI re-picker and µm→px matching tolerance).

| **Column**  | **Meaning**                                                                                                    | **Units / Notes** |   |       |
| ----------- | -------------------------------------------------------------------------------------------------------------- | ----------------- | - | ----- |
| `image`     | The base filename (stem) of the image analyzed (e.g. `P8164337`)                                               | –                 |   |       |
| `actual`    | The total number of **true symbiont cells** in the ROI from your Fiji CellCounter XML                          | count             |   |       |
| `pred`      | The number of **cells detected** by your trained YOLO model in the ROI                                         | count             |   |       |
| `tp`        | **True Positives** — model detections that correctly matched a true cell within the tolerance radius           | count             |   |       |
| `fp`        | **False Positives** — model detections that did *not* correspond to any true cell                              | count             |   |       |
| `fn`        | **False Negatives** — true cells that the model *missed*                                                       | count             |   |       |
| `precision` | Fraction of detections that are correct: `TP / (TP + FP)`                                                      | 0–1               |   |       |
| `recall`    | Fraction of true cells correctly detected: `TP / (TP + FN)`                                                    | 0–1               |   |       |
| `f1`        | Harmonic mean of precision and recall: `2PR / (P + R)`                                                         | 0–1               |   |       |
| `abs_err`   | Absolute error in count: `                                                                                     | pred - actual     | ` | count |
| `diff`      | Signed difference between predicted and actual counts (`pred - actual`)                                        | count             |   |       |
| `tol_px`    | The matching tolerance used for spatial pairing, in **pixels** (converted from your `--match_um` if specified) | pixels            |   |       |

